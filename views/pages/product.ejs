        <section>
            <% if (stock.imageUrl) { %>
                <img src="<%= stock.imageUrl %>" alt="Product Image" width="100">
            <% } else { %>
                No image
            <% } %>
            <h1><%= stock.productName %></h1>
            <hr>
            <h3><%= stock.productCode %></h3>
            <h3><%= stock.category %></h3>
            <h3><%= stock.brand %></h3>
            <h3><%= stock.qty %></h3>
            <h3>£<%= stock.rrp %></h3>
            <h3>£<%= stock.price %></h3>
            <h3><span class="barcodeFont"><%= stock.barcode %></span></h3>
            <br>
            <hr>
            <form action="/delete" method="POST" onsubmit="return confirm('Are you sure you want to delete this item?');">
                <input type="hidden" name="barcode" value="<%= stock.barcode %>">
                <button type="submit">Delete</button>
            </form>
        </section>
