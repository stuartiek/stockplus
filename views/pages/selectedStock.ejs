<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="scripts/stock.js"></script>
    <link rel="stylesheet" href="styles/main.css"/>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Libre+Barcode+39+Text&display=swap" rel="stylesheet">
    <link rel="icon" type="image/x-icon" href="images/logo/favicon.png">
    <link rel="manifest" href="manifest.json">
    <script src="https://kit.fontawesome.com/e71e3c46fe.js" crossorigin="anonymous"></script>
    <title>Stock</title>
</head>
<body>
    <header>
        <%- include('../partials/header') %>
    </header>
    <main id="stock">
        <div class="stockHead">
            <h1>Selected Stock Items</h1>
        </div>
          
          <button class="printBtn" onclick="window.print()">Print</button>
          <br>
          <div class="labelDisplay">
        <% selectedItems.forEach(item => { %>
            <%#
            Dynamically add the labelType as a CSS class.
            We check if 'document' exists and has a labelType.
            .toLowerCase() converts "Shelf" to "shelf", which is better for CSS.
            %>
            <div class="label <%= (document && document.labelType) ? document.labelType.toLowerCase() : '' %>">
                <div class="labelHead">
                    <p class="labelName"><%= item.productName %></p>
                </div>
                
                <div class="labelMid">
                    <h4 class="labelBrand"><%= item.brand %></h4>
                    <h1 class="labelPrice">£<%= item.price %></h1>
                </div>

                <div class="labelFooter">
                    <p class="labelCode"><%= item.productCode %></p>
                    <p class="labelYear">2025</p>
                    <p class="labelBarcode"><%= item.barcode %></p>
                </div>
            </div>
        <% }) %>
</div>
            
    </main>
</body>
</html>